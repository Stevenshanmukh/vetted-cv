# Database Configuration
# Create a free Postgres database on Supabase, Neon, or Railway
DATABASE_URL="postgresql://user:password@localhost:5432/vetted_cv"

# Authentication (NextAuth / Auth.js)
# Generate a secret with `openssl rand -base64 32`
NEXTAUTH_SECRET="changeme"
NEXTAUTH_URL="http://localhost:3000"

# AI Provider Keys (Optional - Add at least one to enable AI features)
OPENAI_API_KEY="sk-..."
ANTHROPIC_API_KEY="sk-ant-..."
GEMINI_API_KEY="AI..."
PERPLEXITY_API_KEY="pplx-..."
